package com.xiaoluozhi.anime.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

//// 定义你想要的更大圆角半径
//val ButtonCornerRadius = 16.dp // 例如，16dp (默认 small 可能是 4dp 或 8dp, medium 可能是 12dp)
//
//// 获取 M3 默认 Shapes
//val DefaultShapes = Shapes()
////基于默认值创建
//val AppShapes = DefaultShapes.copy(
//    small = RoundedCornerShape(ButtonCornerRadius),
//    medium = RoundedCornerShape(ButtonCornerRadius),
//    large = RoundedCornerShape(ButtonCornerRadius)
//)
//
//// 如果你只想修改部分，可以基于默认值创建：
//// val DefaultShapes = Shapes() // 获取 M3 默认 Shapes
//// val AppShapes = DefaultShapes.copy(
////     small = RoundedCornerShape(ButtonCornerRadius),
////     medium = RoundedCornerShape(ButtonCornerRadius)
//// )